Run all cells of the Detection_model.ipynb except the last cell. the last cell is the actual realtime camera feed working cell

The 'senteces' list contains all the detected each time the model works.
